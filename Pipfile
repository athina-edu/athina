[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
athina = {editable = true, path = ".", extras = ["test"]}
pytest = ">=7.0"
pytest-timeout = ">=2.0"
docker = ">=6.0"

[requires]
python_version = "3.14"
